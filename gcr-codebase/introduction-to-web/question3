<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Browser Request and Web Page Display</title>
</head>
<body>
    <!-- Q3 -->
    <h1>How a Browser Requests and Displays a Web Page</h1>
    <ol>
        <li>Enter the website URL in the browser.</li>
        <li>DNS resolves the domain name to an IP address.</li>
        <li>The browser sends an HTTP/HTTPS request.</li>
        <li>The web server processes the request.</li>
        <li>The server returns the requested response/resources.</li>
        <li>The browser parses and renders the page for the user.</li>
    </ol>
    <p><strong>Flow:</strong> URL → DNS → Request → Server → Response → Browser Rendering</p>
</body>
</html>